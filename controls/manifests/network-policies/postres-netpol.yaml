# File: manifests/network-policies/postgres-netpol.yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-netpol
  namespace: vuln-bank-db
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # ONLY accept connections from the vuln-bank application pod
  - from:
    - namespaceSelector:
        matchLabels:
          name: vuln-bank
      podSelector:
        matchLabels:
          app: vuln-bank
    ports:
    - port: 5432
  egress:
  # DNS only — PostgreSQL has no business making outbound connections
  - to:
    - namespaceSelector: {}
    ports:
    - port: 53
      protocol: UDP